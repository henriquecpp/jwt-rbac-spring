services:
  postgres:
    image: postgres:16
    container_name: jwt-rbac-postgres
    restart: always
    environment:
      POSTGRES_DB: jwt_rbac
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - jwt-rbac-network

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: jwt-rbac-app
    restart: always
    depends_on:
      - postgres
    ports:
      - "8080:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/jwt_rbac
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: postgres
    networks:
      - jwt-rbac-network

volumes:
  postgres_data:

networks:
  jwt-rbac-network:
    driver: bridge